{
  "server": "hub-cloud.browserstack.com",
  "user": ${{ secrets.BROWSERSTACK_ACCESS_KEY }},
  "key": ${{ secrets.BROWSERSTACK_USERNAME }},

  "capabilities": {
    "build": "testng-browserstack",
    "name": "single_test",
    "browserstack.debug": true
  },

  "environments": {
    "chrome": {
      "browser": "chrome"
    }
  }
}
